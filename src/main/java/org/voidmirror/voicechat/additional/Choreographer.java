package org.voidmirror.voicechat.additional;

public class Choreographer {
    /* TODO: contains a pool of threads to look after with their construction arguments.
        If thread.isAlive() == false, restart thread with same arguments. */
}
